let test = require('tape');

const opts = {
  target: 'mapdiv'
}

let Lbk = L;
let Gbk = google;
let olbk = ol;

test('nlmaps.createMap configures leaflet if more than one lib is defined', function(t) {;
  t.end();

});

test('nlmaps.createMap configures leaflet if its the only one defined');
test('nlmaps.createMap configures openlayers if its the only one defined');
test('nlmaps.createMap configures google maps if its the only one defined');
